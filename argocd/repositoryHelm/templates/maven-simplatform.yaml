#
# Copyright contributors to the Galasa project 
#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: maven-simplatform
  labels:
    app: maven-simplatform
spec:
  replicas: {{ .Values.replicas }}
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: maven-simplatform
  template:
    metadata:
      labels:
        app: maven-simplatform
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      containers:
      - name: maven-simplatform
        image: {{ .Values.registry }}/galasadev/galasa-maven-simplatform:{{ .Values.branch }}
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        resources:
          requests:
            memory: 50Mi
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - maven-simplatform
              topologyKey: kubernetes.io/hostname
